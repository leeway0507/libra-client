import { Text } from "@chakra-ui/react"

export function SubTitle({children}:{children:React.ReactNode}){
    return <Text fontSize={"xl"} fontWeight={600}>{children}</Text>
}

export function BookTitle({children}:{children:React.ReactNode}){
    return <Text fontSize={"xl"} fontWeight={600}>{children}</Text>
}